---
name: game-spec
description: Define or refine player-visible Barnacle Rescue behavior, state rules, edge cases, and acceptance criteria without implementing by default.
---

# Game Specification

Inspect `docs/game/` and current behavior before editing specifications. Describe observable player behavior first, then state transitions, edge cases, and Given/When/Then acceptance criteria. Update every affected game document without duplicating a rule unnecessarily.

Do not implement code unless explicitly requested. Preserve the MVP boundaries and vertical-slice priority in `docs/game/MVP_SPEC.md`. User instructions override this workflow.
